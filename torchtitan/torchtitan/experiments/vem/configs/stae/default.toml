[job]
dump_folder = "./outputs/vem2_stae"
description = "vem debug model"
print_args = true

[profiling]
enable_profiling = false
save_traces_folder = "profile_trace"
profile_freq = 10
enable_memory_snapshot = false
save_memory_snapshot_folder = "memory_snapshot"

[metrics]
log_freq = 1
disable_color_printing = false
enable_tensorboard = true
save_tb_folder = "tb"
enable_wandb = true

[model]
name = "mesh_stae"
flavor = "base"

[optimizer]
name = "AdamW"
lr = 1e-4
weight_decay = 0.01
betas = [0.9, 0.95]

[lr_scheduler]
warmup_steps = 500  # warmup steps
decay_ratio = 0.1  # no decay, stay stable during training

[training]
batch_size = 8
max_norm = 1.0  # grad norm clipping
steps = 12000
dataset = "stae"
mixed_precision_param = "bfloat16"
mixed_precision_reduce = "float32"
compile = false
seed = 42
num_workers = 15
fsdp_cast_forward_inputs = false

[training.dataset_kwargs]
instance_list = [
    "xxxx"
]
repeats = 1
shuffle_seed = 42
use_bos = true
bos_bucket = "xxxx"
aug_flip = true
aug_rotate_all = true
aug_rotate_z = false
aug_scale = true
aug_scale_range = [0.8, 1.2]
vertex_noise = 1e-3
random_negative = true
infinite = true 

[parallelism]
data_parallel_replicate_degree = 1
data_parallel_shard_degree = -1
context_parallel_degree = 1
enable_compiled_autograd = false

[experimental]
custom_args_module = "torchtitan.experiments.vem.extra_args_stae"

[checkpoint]
enable_checkpoint = true
folder = "ckpts"
interval = 200
model_weights_only = false
keep_latest_k = 2 # keep all checkpoints
load_step = -1 # automatic loading

[activation_checkpoint]
# mode = "full"
mode = "selective"  # ["none", "selective", "full"]
selective_ac_option = "op"  # "int" = ac every positive int layer or 'op', ac based on ops policy

[memory_estimation]
enabled = false